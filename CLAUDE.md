# Claude Code instructions

Treat `AGENTS.md` as the canonical implementation contract and read it before coding.

Work autonomously through the current roadmap item. Do not ask for confirmation for routine implementation choices that are already resolved in the documents. Prefer a complete, tested vertical slice over a large unverified rewrite.

At the end of each session, report:

- changes made;
- tests and checks run;
- limitations discovered;
- the exact next unchecked roadmap item.
